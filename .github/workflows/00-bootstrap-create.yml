name: 00-bootstrap-create
on:
  workflow_dispatch: {}

# Ce workflow est intentionnellement désactivé (pas de steps).
# Exécute bootstrap-create EN LOCAL avec ton profil AWS:
#   cd infra/bootstrap-create && ./run.sh apply
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Bootstrap Create doit être exécuté localement (sans secrets GitHub)."
          echo "Voir infra/bootstrap-create/run.sh"